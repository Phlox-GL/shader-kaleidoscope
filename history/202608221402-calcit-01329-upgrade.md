# Calcit 0.13.29 upgrade verification

- Migrated the application snapshot to the current typed `get`/`Option` field access model and isolated browser File/Promise access behind narrow FFI helpers.
- Updated the dependency graph to the validated Phlox and touch-control migration branches while their PRs are pending.
- Updated the workflow and package compile command to use the explicit current snapshot invocation.
- Verified check-only, JS codegen, Yarn immutable install, and Vite production build with the current Node runtime.
