# Shader kaleidoscope migration follow-up

- Pinned merged Phlox and the current touch-control migration commit while retaining compatible released Respo dependencies.
- Added canonical Snapshot validation and protected deployment from pull requests.
- Verified immutable install, check-only, JS generation, and Vite production build.
